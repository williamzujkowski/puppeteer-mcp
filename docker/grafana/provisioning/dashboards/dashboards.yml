apiVersion: 1

providers:
  - name: 'puppeteer-mcp'
    orgId: 1
    folder: 'puppeteer-mcp'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
